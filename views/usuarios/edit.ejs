<h1>Editar usuário</h1>
<form action="/usuarios/<%= usuario.id %>?_method=PUT" method="POST">
    <div class="form-group">
        <label for="usuarioname">Usuarioname</label>
        <input type="text" class="form-control" id="usuarioname" name="usuarioname" value="<%= usuario.usuarioname %>" required>
    </div>
    <div class="form-group">
        <label for="password">Password</label>
        <input type="password" class="form-control" id="password" name="password" value="<%= usuario.password %>" required>
    </div>
    <div class="form-group">
        <label for="role">Role</label>
        <select class="form-control" id="role" name="role" required>
            <option value="admin" <%= usuario.role === 'admin' ? 'selected' : '' %>>Admin</option>
            <option value="usuario" <%= usuario.role === 'usuario' ? 'selected' : '' %>>Usuario</option>
        </select>
    </div>
    <button type="submit" class="btn btn-warning">Salvar</button>
</form>
